picoCTF{trust_but_verify_2cdcb2de}

1. There is a decypt.sh script already.
2. Iterate each file in folder with
	for file in files/*; do ./decrypt.sh "$file"; done


or
	for file in files/*; do
	  ./decrypt.sh "$file" 2>&1 | grep -v 'Error'
	done
